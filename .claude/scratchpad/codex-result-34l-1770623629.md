EXPLORATION: `Composer` was fixed-height because `SetSize` forced a static textarea height, and `ChatPanel` reserved a hard-coded `8` lines for composer space. Bubble `textarea` supports dynamic resizing (`SetHeight`) and exposes logical content line count via `LineCount`, so auto-resizing in composer update paths is the correct mechanism.

CHANGES:
- `pkg/tui/composer.go`: Added `composerMinLines=1` / `composerMaxLines=6`, added `maxContentHeight` to `Composer`, added `autoResize()`, and called it from `NewComposer`, `SetSize`, `Update`, `SetValue`, and `Reset`. Composer now starts at 1 line and grows/shrinks with content up to 6 lines.
- `pkg/tui/chatpanel.go`: Switched composer init to `NewComposer(6)`, updated `SetSize` to give bounded composer area (`12`), and changed `View()` to compute `historyHeight` from `lipgloss.Height(composerView)` instead of fixed `8`.

BUILD: PASS  
(`GOCACHE=/tmp/go-build-cache go build ./pkg/tui/...` and `GOCACHE=/tmp/go-build-cache go build ./cmd/...` passed; `go vet ./pkg/tui/...` failed on pre-existing `pkg/tui/loghandler.go` copylock warnings unrelated to these edits.)

TESTS: PASS [59 passed, 0 failed]  
(`GOCACHE=/tmp/go-build-cache go test ./pkg/tui/... -v -short -count=1`)

VERDICT: NEEDS_ATTENTION [Feature change is good and validated, but workspace is not globally clean: `go vet` has pre-existing failures and `git diff --stat` includes unrelated existing changes (e.g. `internal/gurgeh/exploration/explore.go`) in addition to the two target files.]