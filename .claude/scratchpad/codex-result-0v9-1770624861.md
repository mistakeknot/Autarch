EXPLORATION: `ChatPanel.SubmitInput()` starts streaming via an unexported `streamStartedMsg`, so dashboard views must forward non-key messages to `chatPanel.Update(msg)` or streaming stalls. All four dashboard views (`gurgeh`, `coldwine`, `pollard`, `bigend`) were only routing key input to chat, and their `Blur()` methods were not canceling active streams.

CHANGES:
- `pkg/tui/claude_handler.go:1` (new): added `ClaudeChatHandler` implementing `ChatHandler`, bridging `pkg/claude.RunStreaming()` events into `StreamMsg` (`TextDelta`, reasoning events, `ToolCallStart`, `StreamDone`, `StreamError`).
- `internal/tui/views/gurgeh.go:146`: forward non-key messages to `v.chatPanel.Update(msg)` with early return when chat emits a command.
- `internal/tui/views/gurgeh.go:352`: added `v.chatPanel.CancelStream()` in `Blur()`.
- `internal/tui/views/coldwine.go:97`: same non-key forwarding pattern.
- `internal/tui/views/coldwine.go:295`: added `CancelStream()` in `Blur()`.
- `internal/tui/views/pollard.go:82`: same non-key forwarding pattern.
- `internal/tui/views/pollard.go:300`: added `CancelStream()` in `Blur()`.
- `internal/tui/views/bigend.go:122`: same non-key forwarding pattern.
- `internal/tui/views/bigend.go:415`: added `CancelStream()` in `Blur()`.

BUILD: PASS

VET: FAIL (`pkg/tui/loghandler.go:140`, `pkg/tui/loghandler.go:151` lock-copy warnings; pre-existing/unrelated)

TESTS: PASS [59 passed, 0 failed]

VERDICT: NEEDS_ATTENTION [Code changes are clean and validated by build/tests, but `go vet` still fails on existing unrelated issues and the repo has unrelated pre-existing dirty changes in other files.]