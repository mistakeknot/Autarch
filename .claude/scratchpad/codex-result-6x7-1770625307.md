EXPLORATION: `GurgehView` already had a `ChatPanel` and submit flow, but no `ChatHandler` was ever set, so browser chat input had no agent backend. The existing Claude streaming pattern is in `pkg/tui/claude_handler.go`, and Gurgeh’s available spec context in this view comes from `*autarch.Client` (`ListSpecs` + `GetSpec`) with fields like `ID`, `Title`, `Status`, `Project`, `Vision`, `Problem`, and `Users`.

CHANGES:
- `internal/tui/views/gurgeh_chat_handler.go`: added new `GurgehChatHandler` implementing `pkgtui.ChatHandler`; it prepends a spec-aware `--system-prompt`, calls `claude.RunStreaming`, and maps `claude.StreamEvent` to `pkgtui.StreamMsg`.
- `internal/tui/views/gurgeh.go`: wired handler into `NewGurgehView` (`SetSpecStore(client)`, `chatPanel.SetHandler(...)`), added `chatHandler` field, added `syncCurrentSpecForChat()`, and update hooks so current spec ID is pushed to the handler on load/selection/nav changes.
- `git diff --stat` reports other pre-existing local modifications in the worktree; this task only changed the two files above.

BUILD: PASS

TESTS: PASS [52 passed, 0 failed]

VERDICT: CLEAN [requested GurgehChatHandler implemented and wired; all required build/vet/test checks succeeded]