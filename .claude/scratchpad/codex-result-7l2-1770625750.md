EXPLORATION: `internal/tui/views/gurgeh_chat_handler.go` and `pkg/tui/claude_handler.go` use the same `claude.RunStreaming` event-to-`pkgtui.StreamMsg` conversion pattern; Gurgeh adds spec-context enrichment on top. `bigend.go`, `coldwine.go`, and `pollard.go` already initialize `ChatPanel` and have chat update/cancel wiring in this working tree, so the remaining work was adding per-view handlers and constructor wiring.

CHANGES:
- `internal/tui/views/bigend_chat_handler.go`: created `BigendChatHandler` stub with Bigend system prompt and Gurgeh-style stream conversion.
- `internal/tui/views/coldwine_chat_handler.go`: created `ColdwineChatHandler` stub with Coldwine system prompt and same stream conversion.
- `internal/tui/views/pollard_chat_handler.go`: created `PollardChatHandler` stub with Pollard system prompt and same stream conversion.
- `internal/tui/views/bigend.go`: added `chatHandler *BigendChatHandler`; in `NewBigendView` instantiate handler, call `chatPanel.SetHandler(...)`, store handler.
- `internal/tui/views/coldwine.go`: added `chatHandler *ColdwineChatHandler`; in `NewColdwineView` instantiate handler, call `chatPanel.SetHandler(...)`, store handler.
- `internal/tui/views/pollard.go`: added `chatHandler *PollardChatHandler`; in `NewPollardView` instantiate handler, call `chatPanel.SetHandler(...)`, store handler.

BUILD: PASS
- `GOCACHE=/tmp/go-build-cache go build ./internal/tui/views/...`
- `GOCACHE=/tmp/go-build-cache go build ./cmd/...`

TESTS: PASS [52 passed, 0 failed]
- `GOCACHE=/tmp/go-build-cache go test ./pkg/tui/... -v -short -count=1`

VERDICT: CLEAN [Requested handler stubs and constructor wiring are in place, build/test checks pass. `git diff --stat` also reports unrelated pre-existing workspace changes outside this task.]