# fd-hci-ratification-cost × fd-assay-mark-integrity (fused) — round 2

## Findings Index
- [P1] r3-r4-hollow-decline-reaffirmed — independently re-derived from card-check.py: f-007 (R3 too strict) is refuted, f-014 (R3+R4 too permissive) holds; this is a re-ask of round-1's already-closed f-023/f-024, not a live disagreement (§card-check.py:172-179,185-195,197-209)
- [P1] uncrancher-hollow-decline-is-structural-not-adversarial — success and guardrail will be declined, not confirmed, on unc-rancher's very first card as a forced consequence of card-check.py's missing-key hard-error plus the measurement's own 0/5 and 4/5 decline rates, so this goal's DONE WHEN #1 will exercise the exact under-scrutinized branch f-014 flagged, on day one, by necessity rather than by exploit (§card-check.py:131-137,172-179 vs proposal measurement table)

## Findings

### r3-r4-hollow-decline-reaffirmed
- **Severity:** P1
- **Where:** `dotfiles/common/.local/bin/card-check.py:172-179` (declined branch), `:185-195` (R4 line), `:197-209` (R3 ratified)
- **What:** Re-deriving from the code directly (not from the prior write-up): R3 (`validate()` line 197-209) computes `drafted = [n for n in FIELDS if states[n] == "drafted"]` and raises only if that list is non-empty. `declined` is not `drafted`, so a card with all five fields `declined` clears R3 with an empty list — `status: confirmed` is legal. R4 (line 185-195) only runs `if line:` — a card that simply leaves `line` unset (falsy) skips the persona/pain-not-declined check entirely, so declining persona and pain too is unblocked. The `declined` branch itself (172-179) requires only a truthy `reason` and a truthy `needs` string per field — no evidence list, no scope, no citation requirement, unlike `drafted`'s R1 (line 153-159, requires ≥1 evidence entry) and R2 (162-168, requires project-scope evidence for `success`/`guardrail`). So a card with five short placeholder strings and a self-attested `confirmed_by`/`confirmed_at` validates cleanly and exits 0 — the sole silent-permit code `guard-plan-needs-card.sh` checks (confirmed independently by reading that script: lines 76-77, `0) exit 0 ;;`, no `--json`/`strength` read anywhere in the file). f-007's claim that ruling 12's gradualism is violated does not hold: ruling 12's text as quoted in the proposal ("backfill on first touch, never bulk... a signal that fires on 100% of rows carries no information") is about *when across 82 projects* a card gets drafted, not about how many of one card's five fields must resolve before its own `status` flips — and nothing in card-check.py prevents resolving fields one at a time across sessions while `status` stays `provisional`. f-014 holds without qualification.
- **Evidence:** Direct read of `card-check.py:150-209` (own analysis, independent of prior round); `guard-plan-needs-card.sh:75-83` (own read, confirms exit-code-only branching). This is the same conclusion round 1 already reached and closed as `f-023`/`f-024` in `round-1/probe-0/fusion-ratification-cost-x-assay-mark-integrity.md` and `heat-ledger.jsonl` — this probe is a re-ask of an already-adjudicated contradiction, not a new disagreement. No new counter-evidence surfaced against f-014 on this pass.
- **Suggestion:** No new suggestion beyond round 1's: require ≥1 field to reach `confirmed` (not merely non-`drafted`) before `status: confirmed` validates, and/or require `declined`'s `needs` to name a concrete unblocking condition. Treat f-007 vs f-014 as closed; a future round should not re-dispatch this pairing without new evidence.

### uncrancher-hollow-decline-is-structural-not-adversarial
- **Severity:** P1
- **Where:** `dotfiles/common/.local/bin/card-check.py:131-137` (missing-field hard error), `:172-179` (declined branch); `docs/proposals/2026-08-13-goal-card-loop-phase2.md` measurement table (uncrancher row: `success` and `guardrail` both `—`)
- **What:** The prior reconciliation (f-023/f-024, and the reaffirmation above) treats the hollow-decline path as an available exploit — something a bad-faith or fatigued confirmer *could* take. The proposal's own measurement table makes it stronger than that for the specific card this goal is about to produce: uncrancher's row shows `success` and `guardrail` both undrafted (`—`), matching the estate-wide pattern (`success` 0/5 drafted anywhere, the field the proposal itself calls "the one an agent cannot produce"). `card-check.py:135-137` hard-errors (`CardError`, exit 2 INVALID) if any of the five `FIELDS` keys is missing from the mapping at all — so the drafter has no option to omit `success`/`guardrail` outright and still reach PROVISIONAL. The only path from INVALID to a validating card is to write `declined` stubs for both, which lands directly in the branch f-014 identified as carrying no evidentiary floor (172-179: truthy `reason` + `needs`, no citation, no scope check — the opposite of R1/R2's rigor for `drafted`). This means 2 of the card's 6 slots (`success`, `guardrail`) will take the under-scrutinized path on unc-rancher's very first real card *by structural necessity*, independent of anyone's diligence or bad faith — and DONE WHEN #1 ("a confirmed `docs/why.md` that `card-check.py` exits 0 on") will read as fully satisfied by a card that is at minimum one-third hollow by design. This sharpens why "does the six-field format survive a real human confirming it" cannot be answered by the exit-0 check alone: two of the six fields structurally bypass the only rigor the format has (citation), and the DONE WHEN clause as written cannot distinguish that outcome from a card where all six fields were genuinely sourced.
- **Evidence:** `card-check.py:135-137` (`missing = [f for f in FIELDS if f not in fields]; if missing: raise CardError(...)`); measurement table in the proposal (`uncrancher` row: `line D, persona D, pain D, cuj D, success —, guardrail —`); proposal's own text, "`success` drafted 0/5... the field the whole premise depends on, and it is the one an agent cannot produce." Cross-checked against `card-check.py:172-179` for the declined branch's requirements.
- **Suggestion:** When writing up the confirm-step cost per DONE WHEN, explicitly record `strength.score`'s confirmed/declined breakdown for unc-rancher's card (not just the pass/fail exit code), and treat `success`/`guardrail`'s decline reasons as the load-bearing test of whether "declined" carries real information here — if those two reasons turn out to be boilerplate, that is itself the "format finding" the goal asks the confirm step to surface, and it will have been produced by necessity rather than choice.

## Verdict
The contradiction is not live: f-014 holds, f-007 is refuted, exactly as round 1 already closed it (`f-023`/`f-024`). Independently re-deriving from `card-check.py` and `guard-plan-needs-card.sh` reproduces the same conclusion with no new counter-evidence, so this pairing should not be re-probed again absent new evidence. The one genuinely new fact this pass adds: the hollow-decline branch is not merely available as an exploit for some future dishonest confirm — the proposal's own measurement table guarantees `success` and `guardrail` will take that exact branch on unc-rancher's first card, because `card-check.py` hard-errors on any omitted field and neither field was draftable there. The goal's DONE WHEN #1 will therefore pass by construction on a card that is one-third hollow by necessity, which the exit-code check alone cannot see.
